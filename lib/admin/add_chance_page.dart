import 'package:flutter/material.dart';

class AddChance extends StatelessWidget {
  const AddChance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Add Chance')),
    );
  }
}
